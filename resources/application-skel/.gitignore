# Real environment values (with secrets) — never committed.
# `.env.example` (without leading dot-env-anything-else) is the committed template.
.env
.env.local

# TLS certs + keys + PF-71 secret env file all live here. Never commit.
/certs/

# Play artifacts
/tmp/
/logs/
/test-result/
/precompiled/
/db/
/lib/
/modules/
*.iml
.idea/
.vscode/
