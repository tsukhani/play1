~ Name:
~ ~~~~~
~ enable-https -- Enable HTTPS on port 9443 with HTTP/2 (ALPN h2) negotiation
~
~ Synopsis:
~ ~~~~~~~~~
~ play enable-https [app_path]
~
~ Description:
~ ~~~~~~~~~~~~
~ Configures the application to listen for HTTPS on port 9443 alongside
~ the plain HTTP listener on port 9000. The framework activates ALL
~ three modern HTTP versions automatically when HTTPS is on:
~
~   • HTTP/1.1 over TLS (ALPN fallback for old clients)
~   • HTTP/2 over TLS (ALPN h2 — modern browsers, curl --http2, etc.)
~   • HTTP/3 over QUIC (UDP same-port; advertised via Alt-Svc header so
~     clients discover it after their first HTTPS request)
~
~ Graceful degradation: HTTP/3 needs the Netty native QUIC library,
~ shipped for osx-{aarch_64,x86_64}, linux-{aarch_64,x86_64}, and
~ windows-x86_64. On linux-riscv64 (or anywhere the native binary won't
~ link), the framework logs a WARN and skips the UDP listener; HTTPS,
~ HTTP/2, and HTTP/1.1 keep working normally on TCP.
~
~ Performs three steps idempotently:
~
~   1. If conf/certificate.jks does not exist, generate a self-signed JKS
~      keystore via keytool: alias=play, RSA 2048, CN=localhost, validity
~      3650 days, password from a 16-byte random base64 string. If the
~      keystore already exists, it is reused (the password is read back
~      out of conf/application.conf).
~
~   2. Set keystore.algorithm, keystore.file, keystore.password, and
~      https.port=9443 in conf/application.conf. Skeleton-style commented
~      lines are uncommented in place. ONLY default values are written —
~      existing active lines (e.g. a custom https.port=8443 or
~      keystore.file=conf/my-cert.jks) are preserved verbatim.
~
~   3. If http.port is not already an active line, set http.port=9000.
~      The framework's special-case "default to 9000 when neither port is
~      set" fallback (Server.java) does NOT fire once https.port is
~      configured, so the explicit http.port line is needed to keep the
~      plain HTTP listener bound. A user-customized http.port (e.g. 8080,
~      or -1 to disable HTTP entirely) is preserved across enable-https
~      runs.
~
~ Run play run or play start afterwards to apply. The startup log will
~ show two listener lines:
~     Listening for HTTP on port 9000 (Waiting a first request ...)
~     Listening for HTTPS on port 9443 (Waiting a first request ...)
~
~ Idempotent: re-running when HTTPS is already enabled is a no-op. The
~ command does NOT regenerate the keystore if one already exists, so
~ disable-https followed by enable-https reuses the same cert.
~
~ The generated keystore is for local development only — for production,
~ replace conf/certificate.jks with a real cert (Let's Encrypt, internal
~ CA, etc.) and update keystore.password in conf/application.conf.
~
~ See also: play disable-https
~
